<?xml version="1.0" encoding="utf-8"?>
<LinearLayout

    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".controller.MainActivity">

    <Button
        android:id="@+id/activity_main_disconnect_btn"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:text="Disconnect"
        android:textSize="10sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:gravity="center"
        android:layout_marginTop="30dp"
        android:textSize="30sp" />

    <Button
        android:id="@+id/activity_main_myproduct_btn"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="100dp"
        android:textSize="20sp"
        android:text="My product"></Button>

    <Button
        android:id="@+id/activity_main_shoppinglist_btn"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="30dp"
        android:textSize="20sp"
        android:text="Shopping List"></Button>

    <Button
        android:id="@+id/activity_main_recipe_btn"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="2"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="30dp"
        android:layout_marginBottom="100dp"
        android:textSize="20sp"
        android:text="Recipes"></Button>

</LinearLayout>